services:
  - type: web
    name: unemployed-simulator-backend-test
    runtime: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 2 -b 0.0.0.0:$PORT backend:app
    healthCheckPath: /health
